# 使用说明

> 这份是「怎么用」；**为什么这么写 / 踩过哪些坑**在 `README.md` 里。
> 所有命令里的 `...\python.exe` 都指
> `C:\Users\tangl\.workbuddy\binaries\python\envs\default\Scripts\python.exe`
> （**必须用这个**，系统里其它 Python 没装 cv2，会直接报 `No module named 'cv2'`）。

---

## 一、30 秒版

| 我想干嘛 | 怎么做 |
|---|---|
| **现在立刻跑一次** | 双击 **`run_now.bat`** |
| **改配置（含绑定后端）** | 双击 **`config_tool.bat`**（全中文菜单，改完直接生效） |
| **看最近一次结果** | 打开 **`https://你的域名`**（后端控制台），或双击 `logs\reports\latest.html` |
| **改任务开关 / 参数** | 后端控制台 → **任务配置**（改完脚本下次启动自动生效，不用碰本机文件） |
| **让脚本下次跑某个任务** | 后端控制台 → **待执行** → 新建请求 |
| **看现在是什么模式 / 后端通不通** | `...\python.exe run_daily.py --status` |
| **临时脱离后端跑一次** | `...\python.exe run_daily.py --offline` |
| **把游戏和模拟器都关掉** | `...\python.exe tools\emu.py kill` |
| **补传本地报告**（网络恢复后） | `...\python.exe run_daily.py --upload-last` |

**没有定时任务。** 什么时候跑由你决定 —— 前几周你在聊天里让我跑（方便我边跑边修 bug），
稳定之后用你自己那套定时开机的机制拉起 `run_daily.bat` 即可。

---

## 一·五、两种运行模式（这个要搞清楚）

脚本既能**被后端托管**，也能**完全不绑后端独立运行**。默认是独立运行。

| | 独立运行（默认） | 后端托管 |
|---|---|---|
| **怎么开启** | 出厂就是；`config.json` 的 `cloud.enabled = false` | `cloud.enabled = true` **且**填好 `base_url` + `token` |
| **临时切换** | 加 `--offline`（压过一切配置） | 环境变量 `STZB_CLOUD_ENABLED=1` + 地址 + 令牌 |
| **拉远端配置** | ✗ | ✓（只拉业务开关） |
| **领待执行任务** | ✗ | ✓ |
| **跑完上传** | ✗ | ✓ |
| **报告** | 只在 `logs\reports\` | 本机 + 后端各一份 |
| **需要联网** | **完全不需要** | 需要（连不上也照跑，只是不上传） |

**几个关键性质：**

- **独立运行是真的一个字节都不外发** —— 不是「连不上就算了」，而是根压根不会去连。
  `--offline` 会把上传、拉配置、领任务三条路全部关掉。
- **后端配不全会自动降级成独立运行**，并在日志里告警（比如只填了地址没填令牌），
  不会卡在那里反复重试。
- **托管模式下后端连不上也不影响跑任务** —— 任务照跑、报告照落本机，
  事后 `--upload-last` 补传。
- 想看当前是哪种模式：`run_daily.py --status`，它会明确写出来并告诉你后端通不通。

> 所以把整个项目拷到另一台电脑，**不配任何后端**，装好 MuMu 和依赖就能直接跑。

---

## 一·六、配置工具（`config_tool.bat`）

**这是个可选工具。** 它只做一件事：读写项目根目录的 `config.json`。
不用它、甚至删掉它，脚本照样跑 —— 所有默认值都在代码里兜着。

双击 **`config_tool.bat`** 打开全中文菜单：

```
  率土之滨自动化 · 本机配置工具
  运行模式：独立运行  —— 不连任何外部服务，报告只留在本机

  1) 后端托管        绑定 / 验证 / 解绑（不做就默认独立运行）
  2) 模拟器与 ADB
  3) 任务开关（六个任务跑不跑）
  4) 招募参数
  5) 市井参数
  6) 税收参数
  7) 特性参数
  8) 演武参数
  9) 贡品礼包
 10) 安全阀
 11) 日志与报告
 12) 查看全部配置（标注来源与是否被服务端接管）
 13) 备份与恢复
  0) 退出
```

### 绑定后端（第 1 项）

选 1 → 填地址 → 填令牌 → **它会当场连一次做完整验证**：

```
· 正在验证 https://stzb.example.com …
  ✓ 连接正常
      服务端      : 率土之滨自动化 · 控制台 1.0.0
      远端配置版本: v2
      可下发段    : recruit、safety、shijing、tasks、yanwu …
  ✓ 已写入 config.json 并启用托管
```

**验证不通过就不会写任何东西**，而且会告诉你差在哪：

| 报错 | 它会告诉你 |
|---|---|
| 401 | 令牌不对，去控制台「设置」页核对，或用 `new-token` 重新生成 |
| 404 | 地址多半写错了 —— 只填 `https://域名`，不要带路径 |
| 连接被拒 | 确认服务器在跑、域名解析对、防火墙放行 443 |
| 证书失败 | 检查域名证书；本机缺 certifi 也会这样 |
| 域名解析不了 | 检查 base_url 里的域名拼写 |

### 几个好用的小细节

- **改哪项都会先自动备份** `config.json`（保留最近 10 份），第 13 项能一键恢复
- **`_说明` 注释不会丢** —— 工具只改你动的那几个键，不重写整个文件
- **会提示「哪些项正在被服务端接管」** —— 那些项改本机是没用的，会被远端覆盖
- 路径填错了会被拦下来（比如 adb 路径不存在），但可以确认后强制保存
- 每项都标了来源：`本地`（你在文件里写过）还是`默认`（走内置默认值）

### 命令行用法（不想点菜单时）

```bat
cd /d E:\Project\率土自动化
set PY=C:\Users\tangl\.workbuddy\binaries\python\envs\default\Scripts\python.exe

%PY% tools\config.py show                          rem 看全部配置
%PY% tools\config.py show --section cloud          rem 只看后端那一段
%PY% tools\config.py set recruit.half_price false  rem 改一项
%PY% tools\config.py get shuishou.max_times        rem 读一项
%PY% tools\config.py reset recruit.half_price      rem 恢复该项出厂默认
%PY% tools\config.py bind --url https://xx --token stzb_xx   rem 绑定并验证
%PY% tools\config.py unbind                        rem 回到独立运行
%PY% tools\config.py verify                        rem 只测连通，不改配置
%PY% tools\config.py backup / restore              rem 备份 / 恢复
```

---

## 二、目录里哪些是「给你用的」

```
E:\Project\率土自动化\
├─ run_now.bat          ★ 双击 = 立刻跑一次 + 自动打开报告（日常最常用）
├─ run_daily.bat          静默启动器（给「定时开机」这类外部触发器调用）
├─ 使用说明.md            本文件
├─ README.md              设计文档 + 踩坑记录（想知道原理看这个）
├─ config.json          ★ 本机可调项（含后端地址与令牌）
│
├─ server\              ★ 后端控制台（部署到你的公网服务器）
│   ├─ README.md          部署手册（Debian + Docker Compose + Caddy 自动 HTTPS）
│   ├─ docker-compose.yml
│   ├─ Caddyfile
│   ├─ .env.example       复制成 .env 再改
│   └─ app\               后端代码
│
├─ logs\
│   ├─ reports\         ★ 报告在这里
│   │   ├─ latest.html      最近一次的完整报告（截图内嵌，双击即看）
│   │   ├─ latest.json      最近一次的结构化结果（给程序读）
│   │   └─ run_<时间>.html   每一轮的历史报告
│   ├─ shots\           每一轮的原始截图（上百分辨率，3MB 一张）
│   ├─ run_<日期>.log    每天一个运行日志，出问题先看它
│   └─ console.log      用 run_daily.bat 跑时的输出
│
├─ state\
│   ├─ last_run.json    「今天这一档跑过没」的记录
│   ├─ remote_config.json  最近一次应用的远端配置（版本号 + 改动清单）
│   └─ run.lock         单实例锁（跑的时候才存在，跑完自动删）
│
├─ stzb\                采集端代码（core / ui / tasks / emulator / report / cloud / remote_config）
├─ tools\               工具：emu.py 手动用；selftest.py 改代码后必跑
└─ config.json          ★ 所有本机可调项都在这里，改完直接生效
```

---

## 三、常见场景

### 1. 立刻跑一次，并看结果

双击 `run_now.bat`。它等于：

```bat
...\python.exe run_daily.py --slot auto --force --open-report
```

跑完浏览器会自动打开报告。窗口里也会打印一份摘要。

### 2. 只试某一个任务（排查时最有用）

```bat
cd /d E:\Project\率土自动化
...\python.exe run_daily.py --only yanwu --force     rem 只跑演武
...\python.exe run_daily.py --only recruit --force   rem 只跑招募
...\python.exe run_daily.py --only all --force       rem 六个全跑
...\python.exe run_daily.py --list                   rem 看有哪些任务
```

### 3. 先看它想点什么，但不真的点（安全预演）

```bat
...\python.exe run_daily.py --slot 12:00 --dry-run
```

只做识别和判断，**一次都不点**。想确认某个新改动会不会乱点，先跑这个。

### 4. 想从「完全退出」开始跑（状态最干净）

```bat
...\python.exe run_daily.py --cold --only all --force
```

会先把 MuMu 彻底关掉再冷启动。代价是每轮多花 1~2 分钟。

### 5. 手动开关模拟器

```bat
...\python.exe tools\emu.py status      rem 看状态
...\python.exe tools\emu.py stop        rem 退游戏 + 关模拟器
...\python.exe tools\emu.py kill        rem 连 MuMu 主程序一起彻底关掉
...\python.exe tools\emu.py start       rem 拉起来（含游戏）
...\python.exe tools\emu.py restart     rem 彻底关了再冷启动
...\python.exe tools\emu.py game-stop   rem 只退游戏，模拟器留着
```

### 6. 让这台机器全天在线（常驻探活，多机管理要开）

`run_daily.py` 一天只跑 00:00 / 12:00 两档，其余 20 多个小时后端是看不到它的 ——
这时「客户端」页会显示掉线，也没法随时指派或探测。解决办法是**另起一个常驻进程**：

```bat
rem 一直挂着（窗口最小化即可）。它只做心跳 + 接指令，不跑任务
...\python.exe agent.py
```

| 参数 | 作用 |
|---|---|
| `--once` | 只跑一轮心跳就退出（排查用） |
| `--interval 30` | 心跳间隔秒数（默认取服务端下发的值） |
| `--no-probe` | 只心跳，不执行探测指令（不想让后台读屏时用） |
| `--quiet` | 少打印 |

日志在 `logs\agent.log`。它和 `run_daily.py` 共用同一个稳定标识
（存在 `state\client.json`），所以后台看到的始终是同一台机器，不会因为重启变成两台。

> 只想管一台机器、又不需要远程指派的话，**不开这个进程也完全没问题** ——
> 每次跑完任务后台上报一次，够看结果了。

### 7. 上传到后端 / 补传

正常情况下跑完会自动上传，不用管。只有这几种情况需要手动：

```bat
...\python.exe run_daily.py --upload-last    rem 网络恢复后，把本地最近一份补传上去
...\python.exe run_daily.py --no-upload      rem 这轮别上传
...\python.exe run_daily.py --no-remote-config  rem 别拉远端配置，只用本地 config.json
```

后端地址和令牌在 `config.json` 的 `cloud` 段。也可以用环境变量覆盖（令牌不必落盘）：

```bat
set STZB_CLOUD_ENABLED=1
set STZB_CLOUD_BASE_URL=https://你的域名
set STZB_CLOUD_TOKEN=stzb_xxxxxxxx
```

### 8. 后端控制台怎么用

部署方法看 **`server\README.md`**。日常就用这几个页面：

| 页面 | 干什么 |
|---|---|
| **总览** | 最近 14 天统计、最近一次结果、客户端在线卡片、待执行请求 |
| **客户端** | 哪台机器在线/掉线、它在哪个账号角色、**指派**账号角色、**一键探测**、改名/停用 |
| **账号角色** | 登记游戏账号（脱敏串）+ 每个账号下的角色 |
| **运行记录** | 历史轮次，可按客户端/账号筛选 |
| **运行详情** | 任务明细 + 环境 + 客户端/账号 + 日志 + 截图 + 内嵌完整报告 |
| **任务配置** | 改任务开关和参数（带版本历史，可回滚） |
| **待执行** | 排一个「下次跑什么」，也可**定向**只给某台机器 |
| **设置** | 轮换令牌、改口令 |

**配置分三类，别搞混：**

| 类型 | 在哪改 | 说明 |
|---|---|---|
| **业务开关** | 后端「任务配置」页 | 任务开关、半价上限、材料清单、安全阀…… 改完脚本下次启动自动拉走 |
| **账号 / 角色** | 后端「账号角色」页 + 「客户端」页指派 | 这是**数据**不是配置：后端说「这台机器去哪个账号哪个角色」，脚本照做 |
| **本机设置** | 本机 `config.json` | adb 路径、MuMu 路径、后端地址与令牌、日志保留、**要不要允许切换** —— 服务端永远覆盖不了这些 |

这么分是有意的：万一你在外面把配置改错了，至少脚本还能连上模拟器、还能上报，
你还有机会改回来。要是连 adb 路径都能被远端改，改错一次你就得跑回机房。

### 9. 管多台机器 / 多个账号（这一节是新增的重点）

先看第 6 节的「让这台机器全天在线」—— `run_daily.py` 一天只跑两档，
其余 20 多小时后端看不到它；想随时指派/探测，就加跑常驻的 `agent.py`。

**后端三步：**

1. **`账号角色` 页** → 建账号，只需填**脱敏串**（登录页显示成什么样就填什么样，
   如 `159****4508`）→ 给账号加角色（角色名/区服/赛季按游戏里显示的填）。
   ⚠️ **要自动切换的账号，必须先在这台采集机的模拟器里手动登录过一次**，
   否则网易登录页的「常用」里没有它，切不过去。系统不存密码。
2. **`客户端` 页** → 选中一台机器 → 指派表单里选账号 → 角色下拉自动过滤成该账号的角色 → 保存。
   **不用动客户端配置**，机器下次心跳（约 30 秒）就会自动切过去。
3. **`客户端` 页「探测」按钮** → 让那台机器读一次屏（文字+截图+设备状态）回报，
   用来远程确认「它现在到底停在哪个界面」。

**在线判定**：客户端每 30 秒心跳一次，超 90 秒没来算掉线。页面上的在线/掉线是
**按时间算出来的**，不是服务端去连客户端 —— 因为后端在公网、客户端在内网，连不进去。
想调就改服务器 `.env` 的 `STZB_HEARTBEAT_INTERVAL` / `STZB_CLIENT_OFFLINE_AFTER`。

**不想让后台切/读屏**：本机 `config.json` 的 `account` 段（`enabled` / `switch_account` /
`switch_role`），或临时加 `run_daily.py --no-switch`。这段是**本机独占**的，服务端改不了。

---

## 四、报告怎么看

打开 `logs\reports\latest.html`，从上往下是：

1. **一行结论** —— 「全部完成」是绿的；「N 项未完成」是红的
2. **四个数字** —— 完成 / 失败 / 跳过 / 截图总数
3. **任务明细表** —— 每个任务的结果、耗时、截图数、以及原因
4. **运行环境** —— 模拟器启动花了多久、游戏启动、进主城、ADB 设备是哪个
5. **启动过程** —— 从检查模拟器到进主城的完整日志
6. **任务详情** —— 每个任务展开可看执行日志 + 截图缩略图

**「跳过」是正常结果，不是失败。** 比如：

| 日志里看到 | 意思 |
|---|---|
| `不在 00:00 档` | 这个任务只在另一个档位跑 |
| `冷却中（21:27:52后可领取）` | 今天已经领过了，明天 00:00 刷新 |
| `只剩付费的「立勋获取」` | 免费次数用完了，脚本**绝不**花玉符 |
| `是玉符价（绿占比 0.053），不买` | 这个材料要玉符，不是铜钱，不买 |
| `没有可点的领取按钮` | 页面上本来就没有领取键（见 README 里的贡品礼包说明） |

### 报告为什么这么大

约 3MB。因为截图是**内嵌**进去的（缩到 900px 宽 + JPEG 压缩），
好处是**把 html 单独拷走也不会裂**。原始大图在 `logs\shots\`。

---

## 五、出问题了怎么查（按症状）

| 症状 | 先看哪里 | 常见原因 |
|---|---|---|
| 报告是「N 项未完成」 | 打开报告，展开那个任务的日志 | 日志最后几行通常直接写了卡在哪 |
| 报告没更新（还是昨天的） | 控制台「运行记录」，或本机 `logs\reports\latest.html` 的日期 | 脚本没跑 / 模拟器起不来 / 游戏进不去 |
| `ADB 连接失败` | `tools\emu.py status` | 模拟器没起来，或端口不对 |
| `模拟器启动失败` | 报告「启动过程」里那几行 | `config.json` 里 `emulator.manager` 路径不对 |
| 卡在「等待模拟器就绪」 | 手动开一次 MuMu 看能不能开 | MuMu 本身有问题（虚拟化被占、要重启） |
| 某个任务反复重试点错位置 | 报告里那个任务的**截图** | 游戏改版了界面（每周三更新后重点看这里），锚点要更新 |
| `No module named 'cv2'` | —— | 用错 Python 了，必须用上面那个 `envs\default` |
| 日志里 `! 后端探活失败` | 浏览器打开 `https://你的域名/healthz` | 服务器挂了 / 域名证书过期 / 本机断网 —— **任务照跑，报告也留在本地**，恢复后 `--upload-last` 补传 |
| 控制台里没有最新一轮 | 本机 `logs\reports\latest.html` 有没有 | 上传失败不影响本机记录；用 `--upload-last` 补 |
| 后端 `/runs/1/report` 打不开 | 服务器上 `data/artifacts/1/` | 报告文件被清理了（按 `STZB_KEEP_RUNS` 保留） |
| 忘了控制台口令 | 服务器上执行 | `docker compose exec app python -m app.cli reset-password` |
| 后台上看到机器「掉线」 | 那台机器 `logs\agent.log` 有没有在刷 | 没跑心跳。起 `agent.py` 常驻，或核对 `cloud` 段的后端地址/令牌 |
| 同一台机器在后台显示成两条 | 两台机器上的 `state\client.json` | 是**整份目录被拷贝**过 —— 删掉副本里的 `state\client.json`，它会重新生成独立标识 |
| 指派改了但机器没切 | 「客户端」页那台的状态字段 | 指派在**下次心跳**（约 30 秒）才生效；确认机器在线、且状态里读到了新账号 |
| 切账号报「常用列表里没有 …」 | 在那台机器的模拟器里手动登一次这个账号 | 网易登录页的「常用」只列**本机登录过**的账号。登一次之后就能免密切换 |
| 换了账号但任务还按老账号跑 | 报告的「运行环境 → 切换结果」 | 切换失败时脚本会**主动中止本轮**（避免跑错账号），看那行原因 |
| 想彻底不让后台切账号/读屏 | 本机 `config.json` 的 `account` 段 / `agent.py --no-probe` | 这段是**本机独占**的，服务端改不了 |
| 令牌换了之后上不了报 | 后端「设置」页的令牌头尾 | 令牌轮换后必须同步改本机 `config.json` |

**排查三件套**：

```bat
...\python.exe run_daily.py --status                     rem 1) 环境对不对
type logs\run_2026-09-19.log                              rem 2) 今天的详细日志
...\python.exe tools\look.py                              rem 3) 现在屏幕上是什么（截图+OCR）
```

`tools\look.py` 会打印当前界面的所有文字和坐标，排查「它为什么不认识这个界面」时最有用。

---

## 六、参数速查

```
--slot auto|00:00|12:00   跑哪一档（auto = 按当前时间）
--only <key>|all          只跑某些任务（key 见 --list）
--force                   忽略「今天这档跑过了」的记录
--dry-run                 只识别不点击
--list                    列出所有任务
--status                  只看状态，什么都不做
--cold                    跑之前先彻底关掉 MuMu，冷启动
--shutdown-after          跑完关掉模拟器
--keep-running            跑完保持模拟器开着
--no-emulator             完全不动模拟器（自己开好了再用）
--open-report             跑完自动打开报告
--offline                 独立运行：完全不连后端（压过 config.json，优先级最高）
--no-upload               本轮不上传到后端（仍会拉配置、领任务）
--no-remote-config        不从后端拉配置，只用本地 config.json
--no-switch               不做账号/角色切换，用当前在线的账号直接跑
--whoami                  打印本机的客户端标识（后台靠它认机器）
--upload-last             不跑任务，只把本地最近的报告补传到后端
```

---

## 七、想改行为，改哪一层

**分清楚再改，改错层会白折腾：**

| 想改什么 | 改哪里 |
|---|---|
| 关掉某个任务 | **后端「任务配置」页**（也可以改本机 `config.json` 的 `tasks.*`） |
| 不抽半价了 | **后端「任务配置」页** → `recruit.half_price` |
| 半价只肯付更少的虎符 | **后端「任务配置」页** → `recruit.half_price_max_hufu` |
| 想买别的材料（用铜钱） | **后端「任务配置」页** → `shijing.buy_materials`（要写游戏里的**准确**名字） |
| 安全黑名单加词 | **后端「任务配置」页** → `safety.never_tap` |
| 后端地址 / 令牌 | **本机 `config.json`** 的 `cloud` 段（服务端下发不了这一段） |
| adb 路径 / 端口 | **本机 `config.json`** 的 `device` 段 |
| MuMu 路径 / 虚拟机索引 | **本机 `config.json`** 的 `emulator` 段 |
| 跑完总是关掉模拟器 | **本机 `config.json`** → `emulator.shutdown_after` = `"always"` |
| 每轮都冷启动 | **本机 `config.json`** → `emulator.cold_restart` = `true` |
| 本机报告保留更久 | **本机 `config.json`** → `logging.keep_days` |
| 服务器上保留更多轮 | 服务器 `.env` → `STZB_KEEP_RUNS` |
| 哪台机器跑哪个账号/角色 | **后端「客户端」页** 指派（是数据，不是配置） |
| 不切账号 / 只切角色 | **本机 `config.json`** → `account` 段，或 `--no-switch` |
| 心跳间隔 / 掉线阈值 | 服务器 `.env` → `STZB_HEARTBEAT_INTERVAL` / `STZB_CLIENT_OFFLINE_AFTER` |

**改完都直接生效**，不用重启模拟器，也不用重装任何东西。

---

## 八、改代码之后必须做的几步

```bat
cd /d E:\Project\率土自动化

rem 0) 脱机测试：不连模拟器、几秒钟跑完，改后端或切换逻辑后必跑
...\python.exe tests\test_account.py           rem 账号/角色切换决策（11 项，无需 opencv）
...\python.exe tests\test_guardrails.py        rem 安全边界：白名单一致 / 不存密码 / 定向任务不越权
...\python.exe tests\smoke_api.py              rem 后端 API 冒烟（需 server 依赖）
...\python.exe tests\e2e_client_server.py      rem 客户端↔服务端联调（真 uvicorn，7 项）

rem 1) 离线自检必须全过（现在 332 项）
...\python.exe tools\selftest.py

rem 2) 真机全量
...\python.exe run_daily.py --only all --force
```

**这几种测试缺一不可** —— 这个项目的真 bug：

- 一半是**自检**抓的（真机看起来完全正常，逻辑其实已错）；
- 一半是**真机**抓的（当时自检全绿，但界面和代码记录的不一致）；
- 还有一批是**脱机测试**抓的，比如「未注册客户端能领走别人的定向任务」这个
  真实漏洞，就是 `e2e_client_server.py` 测出来的。

改了后端（`server\`）的话再加一步：本地起一下看页面能不能打开。

```bat
cd /d E:\Project\率土自动化\server
set STZB_DATA_DIR=E:\Project\率土自动化\.tmp_testdata
...\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8099
```

然后浏览器开 `http://127.0.0.1:8099`，用 `STZB_ADMIN_PASSWORD` 里设的口令登录，
九个页面都点一遍（重点：客户端页的指派/探测、账号角色页的增删）。

> **每周三游戏更新后请重点看**：报告里各任务的截图 + 有没有「反复重试点错位置」，
> 界面改版会让锚点失效，这类问题只能从截图看出来。账号/角色切换的坐标同理 ——
> 更新后先用 `tools\probe_switch4.py` 之类的侦察脚本重新采一次。

---

## 九、接下来的日常节奏

**现阶段（脚本还在磨合）**：你在聊天里说一声，我来跑，跑完我看报告、修问题、改代码。
游戏每周三会更新，界面一改锚点就可能失效，这类问题必须靠**当轮的截图**发现 ——
所以每轮都留着截图和日志，不是浪费。

**稳定之后**：你那边用定时开机的机制拉起 `run_daily.bat`，脚本会自己：

1. 从后端拉最新的任务配置（你在控制台改的开关和参数）
2. 看后端有没有排的待执行请求，有就领走按它跑
3. 冷启动模拟器 → 开游戏 → 进主城
4. 跑六个任务
5. 关掉模拟器，把结果、报告、截图推到后端

你要做的只剩：**偶尔上控制台看一眼**。
